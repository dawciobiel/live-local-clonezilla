# ps
# export PS_FORMAT="pid=PID:cmd=PROCES"

# All aliasses are stored in /etc/profile.d/aliases.sh
# alias ps='ps -e -o pid=PID,cmd=PROCES'
# alias ll="ls -lah --color=always"

# EDITORS
export EDITOR="/usr/bin/vim"

# Add hardclone-cli to PATH variable
export PATH="/opt/hardclone-cli:$PATH"
